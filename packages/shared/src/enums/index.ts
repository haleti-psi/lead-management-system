// Enums — the single source of truth for both apps/api and apps/web.
// GENERATED in Stage 7 from the BRD §5.5 enum catalog (docs/data-model/schema.sql).
// Do not hand-author enum values here; never redefine an enum locally in a module.
// See docs/contracts/shared-utilities.md ("Shared enums").
export {};
