/** Correlation-id wire header (request + response) and CLS/log property key. */
export const CORRELATION_HEADER = 'x-correlation-id';

/** Property name under which the correlation id is bound on the request and log lines. */
export const CORRELATION_ID_KEY = 'correlation_id';
