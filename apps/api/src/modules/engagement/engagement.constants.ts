/**
 * FR-104 — M11 Engagement constants.
 */

/** `configuration_versions.config_type` discriminator for SLA policy changes. */
export const SLA_POLICY_CONFIG_TYPE = 'sla_policy';

/** entity_type used on `audit_logs(config_change)` rows for SLA policies. */
export const SLA_POLICY_ENTITY_TYPE = 'sla_policies';
