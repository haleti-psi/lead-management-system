# Gate C — Contracts Package Completeness Review

**Date:** 2026-06-09
**Gate:** C (Stage 5 → Stage 6). Blocks **Stage 6 (LLD generation)**.
**Checked by:** `quality-gate-checker`
**Artefacts reviewed:** `docs/contracts/` (10 files) cross-referenced against `docs/brd.md` (v5.2), `docs/data-model/` (47 entities), `docs/architecture.md`, `docs/guidelines/`.
**Machine validation:** `api-contract.yaml` parsed with **`npx js-yaml` (Node v24)**; `auth-matrix.json` parsed with PowerShell `ConvertFrom-Json`.

---

## Verdict: ✅ PASS (10 / 10) — signed off. Stage 6 (LLD generation) unblocked.

| | Count |
|---|---|
| ✓ Pass | 10 / 10 |
| ⚠ Warn | 0 |
| ✗ Fail | 0 |

> **A real defect was caught and fixed during this gate.** The first `js-yaml` parse **failed** — `x-fr-coverage` used YAML flow sequences (`[ … ]`) containing `{id}`/`{token}`, which YAML parses as flow-mappings → "missed comma between flow collection entries". The values were converted to quoted strings; re-parse is clean (exit 0, 62 paths). This is genuine validation, not a self-attestation.

## Checklist Results

### ✓ 1. Every FR has ≥1 entry in api-contract.yaml — PASS
**49/49 FRs** present (each operation carries an `x-frs` tag + a top-level `x-fr-coverage` map). Verified by extracting `FR-NNN` from the BRD and matching each in the YAML.

### ✓ 2. Every BRD role appears in auth-matrix.json — PASS
All **9 roles** (RM, BM, SM, HEAD, KYC, DPO, PARTNER, ADMIN, CUSTOMER) in `_meta.roles` and `capability_matrix` (capability × scope per §3.3).

### ✓ 3. Every data-model resource appears in auth-matrix.json — PASS
**47/47** resources in `_meta.resources` (46 BRD entities + `orgs`); `partners` added during this gate (was missing from the list). `resource_governance` maps each to its governing capability + owning module + `system_managed` flag.

### ✓ 4. Every entity with a status enum has a state-machine definition — PASS
**22/22** status enums covered in `state-machines.md` (detailed: lead_stage, doc_status, kyc_check_status, consent_state, grievance_status, config_change_status, integration_status, task_status; compact: user/grant/dup_record/link/rights/partner/config/eligibility/job/outbox status). Added during this gate: `delivery_status`, `scan_status`, `validation_status`, `attribution_status`.

### ✓ 5. Every external service has a named test-double strategy — PASS
`integration-map.md` covers LOS (`LosPort` + `LosMockAdapter`), KYC (PAN/CKYC/DigiLocker/Aadhaar/V-CIP via `KycPort`), Communication (SMS/WhatsApp/Email), Telephony/CTI, AA, GST, VAHAN/RTO, Bureau-via-LOS, Campaign — each with failure modes + a test-double, plus a test-double summary (mock adapters; never real providers in tests).

### ✓ 6. Every error type has a defined handling strategy — PASS
`error-taxonomy.md`: 10 primary codes (§8.4; `VALIDATION_ERROR=400`) + 7 domain sub-reasons, each with HTTP status, user-visibility, alert flag, retryable, and "Handling rules" (exception filter, upstream→queue, optimistic-lock→409, append-only enforcement, per-code test requirement).

### ✓ 7. All environment variables listed in environment-contract.md — PASS
Required / optional(defaults) / provider vars derived from architecture + integrations; Secret Manager only; startup Zod validation. (No source code yet to cross-reference; list is complete against the architecture and integration map.)

### ✓ 8. Shared utilities present — PASS
`shared-utilities.md` inventories core services (EntitlementService, UnitOfWork, **LeadService mutators**, AuditAppender, OutboxService, IntegrationGateway+ports, BusinessCalendar/SlaEngine, MaskingService), shared Zod schemas, guards/middleware, and UI primitives — with "reuse, don't recreate".

### ✓ 9. No two FRs share a state-machine transition with incompatible side effects — PASS
The shared machine (Lead `lead_stage`) is single-owned: all transitions go through `StageGuardService` + `LeadService.transitionStage` (one transaction writing leads + stage_history + audit + outbox). Other entities have disjoint machines. No conflicting side effects.

### ✓ 10. api-contract.yaml is valid YAML with required fields per endpoint — PASS
**`npx js-yaml` exit 0** (after the flow-sequence fix); parses to 62 paths. Every operation has `summary`, `operationId`, `tags`, `security`, and `responses`; common components (envelope, error, parameters, responses) defined; uniform `{data, meta, error}` envelope and §8.4 error responses referenced throughout.

---

## Fixes applied during this gate
1. **YAML validity:** converted `x-fr-coverage` flow sequences → quoted strings (the `{id}`/`{token}` braces broke flow parsing). Re-validated clean.
2. **auth-matrix:** added `partners` to `_meta.resources` (47/47).
3. **state-machines:** added `delivery_status`, `scan_status`, `validation_status`, `attribution_status`.

## Sign-off
- **Verdict:** PASS (10/10). Recorded in `manifest.json` as `gates.C.signed_off = true`.
- **Stage 6 (LLD generation) is unblocked.** Next: `/lld-generator`.

## Caveats carried forward (tracked; not gate blockers)
- **OD-08 / OD-17:** PAN/CKYC/DigiLocker/comms/CTI vendor selection and TRAI-DLT/WhatsApp-WABA registration remain open — adapters are built against the ports (`integration-map.md`) regardless; resolve before integration go-live.
- **OD-06 (Legal/DPO):** consent-bootstrap lawful basis for the first outbound message — flagged by the council; resolve before customer self-service go-live.

## Verification provenance
`npx js-yaml` (Node v24.14.1) → api-contract.yaml valid (exit 0, 62 paths). `ConvertFrom-Json` → auth-matrix.json valid (9 roles, 47 resources, 14 public endpoints). FR coverage 49/49 and status-enum coverage 22/22 verified by cross-reference against `docs/brd.md` and `docs/data-model/schema.sql`.
